# Database
DATABASE_URL="postgresql://postgres:password@localhost:5432/galaxy?schema=public"
# Optional (recommended in production if you use a pooled DATABASE_URL).
# For local dev you can set this equal to DATABASE_URL.
DIRECT_URL="postgresql://postgres:password@localhost:5432/galaxy?schema=public"

# NextAuth
# Local dev:
NEXTAUTH_URL="http://localhost:3000"
# Generate with: `openssl rand -base64 32`
NEXTAUTH_SECRET=""

# Google OAuth (Google Cloud Console -> OAuth 2.0 Client IDs)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# Email login (magic links)
# Format example:
# EMAIL_SERVER="smtp://USER:PASS@smtp.mailgun.org:587"
EMAIL_SERVER=""
EMAIL_FROM="GALAXY <no-reply@example.com>"

# Cloudinary (signed uploads)
CLOUDINARY_CLOUD_NAME=""
CLOUDINARY_API_KEY=""
CLOUDINARY_API_SECRET=""
